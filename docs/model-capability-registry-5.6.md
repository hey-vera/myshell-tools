# Model Capability Registry 5.6 — historical compatibility pointer

The original design document was archived at `docs/archive/model-capability-registry-5.6.md`. Existing source comments and historical changelog entries refer to this path, so this pointer preserves link integrity without pretending the archived design is the current build authority.

Current implementation authority:

- `CLAUDEPLAN.md` R1: per-account execution-lane inventory and adapter contract.
- `CLAUDEPLAN.md` R2: safe same-chat model/account adaptation.
- `CLAUDEPLAN.md` R5: evidence-backed routing, context, quota, and outcome learning.

Where the archived design conflicts with `CLAUDEPLAN.md`, the latter wins.
